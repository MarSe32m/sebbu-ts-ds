import XCTest
@testable import sebbu_ts_ds

final class sebbu_ts_dsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(sebbu_ts_ds().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
